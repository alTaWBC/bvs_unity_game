import React from "react";
import styles from "./footer.module.css";

const footer = (props) => (
    <div className={styles.Footer}>
        <span> &copy;</span> BioVisualSpeech
    </div>
);

export default footer;
